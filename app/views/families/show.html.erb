<% provide(:title, @family.family_name) %>
<p id="notice"><%= notice %></p>

<p>
  <h2>The <strong><%= @family.family_name %></strong> Family</h2>
</p>

<% if primary_exists == "" %>
  <div id="notice">NO PRIMARY ADULT SELECTED, PLEASE CHOOSE A PRIMARY ADULT</div><br>
<% else %>
  <div class="field">
  The <strong>Primary Adult</strong> is <strong><%= primary_exists %></strong>.
  </div>
<% end %>

<p>
  <strong>Street address:</strong>
  <%= @family.street_address %>
</p>

<p>
  <strong>City:</strong>
  <%= @family.city %>
</p>

<p>
  <strong>State:</strong>
  <%= @family.state %>
</p>

<p>
  <strong>Zip:</strong>
  <%= @family.zip %>
</p>

<p>
  <strong>Created:</strong>
  <%= @family.created_at %>
</p>
<hr />
<% if @family.people.any? %>
  <h3>People in Family (<%= @family.people.count %>)
  <%= link_to image_tag('add.png'), new_family_person_path(@family), title: "Add New Person to Family" %></h3>
  <table>
    <thead>
      <tr>
        <th>Name</th>
        <th>Birthday</th>
        <th>Primary Adult?</th>
        <th>Preferred Pronouns</th>
        <th>Time Created</th>
        <th colspan="3"></th>
      </tr>
    </thead>
    <tbody>
      <%= render @people %>
    </tbody>
  </table>
<% end %>

<%= link_to 'Edit', edit_family_path(@family) %> |
<%= link_to "Back to List of Families", families_path %>
