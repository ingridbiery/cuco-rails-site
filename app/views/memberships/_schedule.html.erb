<% fees = @membership.family_fees %>
<p>Fees: <%= "$%.2f" % fees.round(2) %>
<% if fees != 0 %>
  Electronic Payment Fee: <%= "$%.2f" % Membership.electronic_payment_fee(fees).round(2) %>
  Total: <%= "$%.2f" % (fees.round(2) + Membership.electronic_payment_fee(fees).round(2)) %>
  PayPal to fees@columbusunschoolers.com
<% end %>
</p>

Volunteering: <%= @membership.family_jobs %>
Unassigned: <%= @membership.family_unassigned %><br /><br />

<% last_period_id = -1 %>
<% signups.sort_by {|signup| [signup.course.period.start_time,
                              signup.person.name]}.each do |signup| %>
  <% if signup.course.period.id != last_period_id %>
    <div class="period"><%= signup.course.period.name %> (<%= signup.course.period.duration_text %>)</div>
    <% last_period_id = signup.course.period.id %>
  <% end %>
  <span class="person"><%= signup.person.name %></span><br />
  <span class="course"><%= signup.course.name %></span><br />
  <span class="role"><%= signup.course_role.name.titleize %></span><br /><br />
<% end %>
