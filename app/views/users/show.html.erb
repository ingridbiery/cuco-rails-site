<p id="notice"><%= notice %></p>

<table>
  <%= render "user_header" %>
  <tbody>
    <%= render @user %>
  </tbody>
</table>

<%= link_to 'Edit', edit_user_registration_path(@user) %> |
<%= link_to 'Back', users_path %>
