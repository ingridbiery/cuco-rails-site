<%= content_for :page_title, "Editing #{@pronoun.preferred_pronouns}" %>

<h1>Editing <%= @pronoun.preferred_pronouns  %> Pronouns</h1>

<%= render 'form' %>

<%= link_to 'Show', pronouns_path %> |
<%= link_to 'Back', :back %>
