<%= content_for :page_title, "New Event" %>

<h1>Create New Event</h1>

<%= form_for [@event] do |f| %>
  <%= render 'form', f: f, single_event: true %>
  <div class="">
    <%= f.submit %>
  </div><br />
<% end %>

<%= link_to 'Back', :back %>