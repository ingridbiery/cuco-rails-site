<%= form_for(@volunteer_job_type) do |f| %>
  <% if @volunteer_job_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@volunteer_job_type.errors.count, "error") %> prohibited this volunteer_job_type from being saved:</h2>

      <ul>
      <% @volunteer_job_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
