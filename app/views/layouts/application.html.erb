<!DOCTYPE html>
<html>
  <head>
    <%= render 'application/favicon' %>

    <meta name="viewport" content="width=device-width, user-scalable=no">
    <title><%= yield(:page_title) %> - Columbus Unschoolers Cooperative (CUCO)</title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>

    <!-- Code needed for IE versions 9 or less -->
    <%= render 'layouts/shim' %>

  </head>

  <body>
    <!-- Code for header on all pages -->
    <%= render 'layouts/header' %>
    
    <!-- For devise -->
    <% if notice %>
      <p class="notice"><%= notice %></p>
    <% end %>

    <% if alert %>
      <p class="alert"><%= alert %></p>
    <% end %>


<p class="alert">We are currently experiencing an issue with processing PayPal payments. We are receiving your payment, but the website is not automatically updating your status as a member. We are updating manually as quickly as we can. We appreciate your patience. If it has been more than 24 hours since you paid and you are still not able to sign up for classes, please contact <%= mail_to "cucohelp@gmail.com" %>.</p>
    <!-- Body of each page -->
    <div class="container">
      <%= yield %>
    </div>
    
    <!-- Code for footer on all pages -->
    <%= render 'layouts/footer' %>

    <!-- debug information, but only if in development -->
    <%= debug(params) if Rails.env.development? %>
  </body>

</html>    

