<p id="notice"><%= notice %></p>

<p>Welcome <%= @user.name %></p>
<p>Your email <%= @user.email %> is
<% if !@user.notification_list? %> not <% end %>
on the notification list</p>
<% if @user.person != nil %>
<p>Your family is <%= link_to @user.person.family.name, family_path(@user.person.family) %></p>
<% else %>
<p>Enter your <%= link_to "family", new_family_path %> info</p>
<% end %>

<%= link_to "Edit", edit_user_registration_path(@user), class: "btn btn-sm btn-primary" %>
