<%= content_for :page_title, "#{@cuco_session.name} Schedule for the 
    #{@membership.family.name} Family" %>

<%= render 'schedule' %>

<%= link_to 'Back to session', @cuco_session %> | <%= link_to 'Back to family', @membership.family %>