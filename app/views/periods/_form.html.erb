<%= form_for [@period] do |f| %>
  <% if @period.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@period.errors.count, "error") %> prohibited this period from being saved:</h2>

      <ul>
      <% @period.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :start_time %>
    <%= f.time_field :start_time %>
  </div>
  <div class="field">
    <%= f.label :end_time %>
    <%= f.time_field :end_time %>
  </div>
  <div class="field">
    <%= f.label :required_signup %>
    <%= f.check_box :required_signup %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
